{
  "name": "deploy-docker-webhook",
  "version": "1.0.0",
  "description": "Run a docker stack deploy on recieving a webhook",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js"
  },
  "keywords": [
    "docker",
    "webhook",
    "nodejs"
  ],
  "author": "Ohffs",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^1.30.1",
    "express": "^4.16.4"
  }
}